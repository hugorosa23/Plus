import React from 'react';
import { View, Text } from 'react-native';

const Home = props => (
    <View style={{ marginTop: 100 }}>
        <Text>Home Page do APP</Text>
    </View>
);

export default Home;